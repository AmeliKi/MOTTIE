# Core
python-dotenv

# Camera / image
depthai==2.24.0.0
opencv-python==4.8.1.78
numpy==1.24.3
pillow

# Visualisation (optional, used by the Jupyter notebook)
matplotlib

# Raspberry Pi GPIO / Neopixel LEDs
adafruit-blinka
adafruit-circuitpython-neopixel

# Azure (IoT Hub + Data Lake / Blob storage)
azure-iot-device~=2.12.0
azure-identity
azure-storage-file-datalake
azure-core

# Optional ML inference (only required when ENABLE_INFERENCE=true)
# onnxruntime
# tensorflow
